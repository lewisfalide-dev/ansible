all:
  children:
    webservers:
      hosts:
        web01:
          ansible_host: 51.250.43.141
          ansible_user: user        # замените на фактического пользователя
          ansible_password: ridhdiy    # замените на фактический пароль
